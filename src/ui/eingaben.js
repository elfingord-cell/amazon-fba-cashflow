export async function render(root){
  root.innerHTML = `
    <section class="card">
      <h2>Eingaben</h2>
      <p class="muted">Formulare (Umsatz, Payout, Extras) werden hier eingebunden. Dieses Modul ist ein Platzhalter.</p>
    </section>`;
}
