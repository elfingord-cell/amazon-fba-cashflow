export async function render(root){
  root.innerHTML = `
    <section class="card">
      <h2>Plan</h2>
      <p class="muted">Zeitplan / Meilensteine – Platzhalter. Inhalte folgen in den nächsten Inkrementen.</p>
    </section>`;
}
